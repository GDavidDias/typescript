"use strict";
(() => {
    var _a;
    const batman = "batman";
    const linterna = 'linterna verde';
    const volcan = `Heroe: volcan negro`;
    const abc = 123;
    console.log(` I'm ${volcan}, ${abc}`);
    console.log(batman.toUpperCase());
    console.log(((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || 'No esta presente');
})();
